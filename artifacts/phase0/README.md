# Phase 0 artifacts

This directory contains machine-generated validation evidence only.

- `pytest-junit.xml`: raw structured pytest result from the reference gate run.
- `remote-validation.json`: machine-readable environment and gate summary.

Phase 0 has no scientific dataset, score table, or figure. Later phases must
store raw score CSV/Parquet and plotting inputs in their own artifact directory.
